public class App {

    /* Очередь (Queue) - такая структура данных, которая позволяет с одной стороны добавить элемент, 
    а с другой удалить (FIFO). */

    // void addFirst(E elt);
    // E removeLast();
    // E getLast();
    // ..
    
    /* Стек (Stack) - така структура данных, которая похволяет с одной стороны добавить элемент, 
    и с той же стороны его удалить (LIFO) */

    // void addFirst(E elt);
    // E removeLast();
    // E getLast();

    /* Двусторонняя очередь (Deque) - это структура данных, сочетающая в себе функциональность очереди и стека */

    public static void main(String[] args) {
        System.out.println("Hello, World!");
    }
}
